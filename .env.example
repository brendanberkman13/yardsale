# Copy to .env and fill in. .env is git-ignored; .env.example is committed.
# Used by docker-compose.yml at this same directory.

# Same value as AUTH_GOOGLE_ID in frontend/.env.local
GOOGLE_CLIENT_ID=
